<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.coolweather.com.coolweather" >
    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:allowBackup="true"
        android:icon="@drawable/coolweatherlogo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="app.coolweather.com.coolweather.activity.ChooseAreaActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="app.coolweather.com.coolweather.activity.WeatherActivity"></activity>
        <service android:name="app.coolweather.com.coolweather.service.AutoUpdateService"></service>
        <receiver android:name="app.coolweather.com.coolweather.receiver.AutoUpdateReceiver"></receiver>
    </application>

</manifest>
